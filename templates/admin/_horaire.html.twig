   {{ form_start(formHoraire) }}
                        <div class="col option">{{ form_row(formHoraire.Jour)}}</div>
                        <div class="col option">{{ form_row(formHoraire.heureMatinOuverture)}}</div>
                        <div class="col option">{{ form_row(formHoraire.heureMatinFermeture)}}</div>
                        <div class="col option">{{ form_row(formHoraire.heureApresMidiOuverture)}}</div>
                        <div class="col option">{{ form_row(formHoraire.heureApresMidiFermeture)}}</div>
                        
                        <div class="form-group">
                            <button class="btnValide ml-3">{{ button| default('Envoyer') }}</button>
                        </div>
					{{ form_end(form) }}