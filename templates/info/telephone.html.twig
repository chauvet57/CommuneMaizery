{% extends 'base.html.twig'%}

{% block body %}

    <div class="card container col-6 mt-5 card-style">
{{ form_start(form) }}
        <h1 class="text-center mt-3">Modifier le téléphone</h1>
            <div class="card-title text-center  homeBanniere">
                
            </div>

        <div class="row text-center">
            <div class="col option container col-5">{{ form_row(form.Village)}}</div>
            <div class="col option container col-5">{{ form_row(form.numeroTelephone)}}</div>
        </div>                   
        <div class="form-group text-center">
            <button class="btnValide ml-3">{{ button| default('Valider') }}</button>
        </div>
    </div> 
{{ form_end(form) }}
<h1></h1>

{% endblock %}

