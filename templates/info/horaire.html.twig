{% extends 'base.html.twig'%}

{% block body %}

<h1>Modifier les horaires</h1>
<div class="container mt-4">   
   
   {{ form_start(form) }}
                        <div class="col option">{{ form_row(form.Jour)}}</div>
                        <div class="col option">{{ form_row(form.heureMatinOuverture)}}</div>
                        <div class="col option">{{ form_row(form.heureMatinFermeture)}}</div>
                        <div class="col option">{{ form_row(form.heureApresMidiOuverture)}}</div>
                        <div class="col option">{{ form_row(form.heureApresMidiFermeture)}}</div>
                        
                        <div class="form-group">
                            <button class="btnValide ml-3">{{ button| default('Valider') }}</button>
                        </div>
					{{ form_end(form) }}
</div>

{% endblock %}